<?php
/**
 * Created by PhpStorm.
 * User: wes
 * Date: 2/24/18
 * Time: 11:34 AM
 *
 * The site root.
 *
 * @var \app\controllers\BaseController $this
 */
$title = 'Home';
$js_init = "";
$container_class = 'container';
$container_id = 'container';
include "app/views/_header.phtml"
?>
    <style>
        .carousel-inner img {
            width: 100%;
            max-height: 600px;
        }
    </style>

    <div class="jumbotron mt-4">
        <!-- Bootstrap Carousel: https://getbootstrap.com/docs/4.0/components/carousel/ -->
        <div id="bsCarousel" class="carousel slide" data-ride="carousel">
            <!-- indicators -->
            <ol class="carousel-indicators">
                <li data-target="#bsCarousel" data-slide-to="0" class="active"></li>
                <li data-target="#bsCarousel" data-slide-to="1"></li>
                <li data-target="#bsCarousel" data-slide-to="2"></li>
            </ol>
            <!-- carousel images -->
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="<?= $_ENV['SUBDIRECTORY'] ?>/public/img/war1.JPG" id="bannerPhotos" alt="Photo of WW2">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>Read Their Stories</h5>
                    </div>
                </div>
                <div class="carousel-item">
                    <img style="" src="<?= $_ENV['SUBDIRECTORY'] ?>/public/img/war2.JPG" id="bannerPhotos2" alt="Photo of WW2">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>Follow Their Journey</h5>
                    </div>
                </div>
                <div class="carousel-item">
                    <img src="<?= $_ENV['SUBDIRECTORY'] ?>/public/img/war3.JPG" id="bannerPhotos3" alt="Photo of WW2">
                    <div class="carousel-caption d-none d-md-block">
                        <h5>Learn history - in a new way</h5>
                    </div>
                </div>
            </div>
            <!-- controls -->
            <a class="carousel-control-prev" href="#bsCarousel" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#bsCarousel" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>

        <h1 class="display-5 my-4">In Their Own Words: World War II</h1>
        <p class="lead">Follow the journey of 786th Battalion as they fought for the Allied Powers during World
            War II.</p>
        <hr class="my-4">
        <p>Read, in their own words, the struggles they went through.</p>
        <p>Create an account today to begin your journey.</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="<?= $_ENV['SUBDIRECTORY'] ?>/about" role="button">Learn more</a>
        </p>
    </div>
    <?php if ($this->getLoggedInUser() != null): ?>
    <section class="col col-12 col-lg-8">
        <h3 class="my-4">Activity Feed</h3>
        <ul class="list-unstyled">
            <?php foreach ($events as $event): ?>
                <?php if ($event->getEventType() == 'comment'): ?>
                    <li class="alert alert-secondary">
                        <h5 class="alert-heading">
                            <?= $event->getUser()->getFullName() ?> commented on a company page:
                        </h5>
                        <p><?= $event->getText() ?></p>
                        <hr>
                        <span><?= \lib\timeAgo($event->getTimestamp()) ?></span>
                        <a href="<?= $this->url('/companies/' . $event->getUnitID()) ?>" class="d-block-inline alert-link ml-4">Read</a>
                    </li>
                <?php elseif ($event->getEventType() == 'message'): ?>
                    <li class="alert alert-primary">
                        <h5 class="alert-heading"><?= $event->getUser()->getFullName() ?> sent you a message:</h5>
                        <p><?= \lib\ellipsize($event->getText(), 100) ?></p>
                        <hr>
                        <div>
                            <span><?= \lib\timeAgo($event->getTimestamp()) ?></span>
                            <a href="<?= $this->url('/messages/' . $event->getId()) ?>" class="d-block-inline alert-link ml-4">Read</a>
                        </div>
                    </li>
                <?php endif; ?>
            <?php endforeach; ?>
            <a href="#" class="btn btn-secondary">Back to top</a>
        </ul>
    </section>
    <?php endif; ?>
<?php include "app/views/_footer.phtml" ?>
